# Drop-in: give the weston system service access to the docker socket so its
# panel launchers (children of the weston process) can run `docker`. Cleaner
# than adding the weston user to the docker group globally.
[Service]
SupplementaryGroups=docker
